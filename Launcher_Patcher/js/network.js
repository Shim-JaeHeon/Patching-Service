
/*

let socket;

function setup() {
  socket = io.connect("http://localhost:3000"); 
  socket.on("connect", function() {
      let delivery = new Delivery(socket);
  })
}

function draw() {
    
}
*/